#ifndef RABIN_KARP_H
#define RABIN_KARP_H

#include <vector>
#include <string>
#include <map>
using namespace std;

map<string, vector<int>> RabinKarpMulti(string text, vector<string> patterns);

#endif